#!/bin/sh
set -e
# Automatically added by dh_installinit
if ([ -x "/etc/init.d/bluetooth" ] || [ -e "/etc/init/bluetooth.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d bluetooth stop || exit $?
fi
# End automatically added section
