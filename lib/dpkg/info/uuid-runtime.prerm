#!/bin/sh
set -e
# Automatically added by dh_installinit
if ([ -x "/etc/init.d/uuidd" ] || [ -e "/etc/init/uuidd.conf" ]) && \
   [ "$1" = remove ]; then
	invoke-rc.d uuidd stop || exit $?
fi
# End automatically added section
